spring:
  jpa:
    hibernate:
      ddl-auto: update
      
    show-sql: true
    
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/classicmodels
    username: newuser
    password: password
        
