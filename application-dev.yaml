spring:
 jpa:
   hibernate:
     ddl-auto: update
     
   show-sql: true
 datasource:
  url: jdbc:mysql://localhost:3306/classicmodels
   
   properties:
     hibernate:
       dialect: org.hibernate.dialect.MySQL5Dialect
