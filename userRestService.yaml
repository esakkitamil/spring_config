spring:
  jpa:
    show-sql : true
    
  messages:
    basename: esakki
      
    hibernate:
      ddl-auto: update
      
    properties:
      hibernate:
        dialect : org.hibernate.dialect.MySQL5Dialect
        
#   datasource:
#     url: jdbc:mysql://localhost:3306/classicmodels
#     username: newuser
#     password: password
    
welcome:
  messages: userRestService_welcome
